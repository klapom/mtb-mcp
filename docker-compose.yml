# mtb-mcp lokale Infrastruktur
#
# Aktuell keine eigenen Container nötig:
# - SearXNG: shared instance unter projects/searxng/ auf Port 8888
#   mtb-mcp übergibt engines=google,bing,duckduckgo per Request-Param.
# - BRouter: kein ARM64-Image verfügbar; ORS wird als Router verwendet.
#
# Falls BRouter wieder aufgesetzt werden soll (x86):
#   services:
#     brouter:
#       image: ghcr.io/abrensch/brouter
#       container_name: mtb-brouter
#       ports: ["17777:17777"]
#       volumes: [brouter-data:/brouter/misc/segments4]
#       environment: [JAVA_OPTS=-Xmx512m]
#       restart: unless-stopped
#   volumes:
#     brouter-data:
